package com.promedicus.frontend;

import static org.assertj.core.api.Assertions.assertThat;

import com.promedicus.frontend.model.Admission;
import org.junit.Test;

public class AdmissionUnitTest {
    /*
    @Test
    public void whenCalledGetName_thenCorrect() {
        Admission admission = new Admission("Julie", "julie@domain.com");
        
        assertThat(admission.getName()).isEqualTo("Julie");
    }
    
    @Test
    public void whenCalledGetEmail_thenCorrect() {
        Admission admission = new Admission("Julie", "julie@domain.com");
        
        assertThat(admission.getEmail()).isEqualTo("julie@domain.com");
    }
    
    @Test
    public void whenCalledSetName_thenCorrect() {
        Admission admission = new Admission("Julie", "julie@domain.com");
        
        admission.setName("John");
        
        assertThat(admission.getName()).isEqualTo("John");
    }
    
    @Test
    public void whenCalledSetEmail_thenCorrect() {
        Admission admission = new Admission("Julie", "julie@domain.com");
        
        admission.setEmail("john@domain.com");
        
        assertThat(admission.getEmail()).isEqualTo("john@domain.com");
    }
    
    @Test
    public void whenCalledtoString_thenCorrect() {
        Admission admission = new Admission("Julie", "julie@domain.com");
        assertThat(admission.toString()).isEqualTo("Admission{id=0, name=Julie, email=julie@domain.com}");
    }
    */
}
